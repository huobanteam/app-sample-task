import { TaskResource, TaskProjectResource, TaskCommentResource, TaskFollowResource, TaskStreamResource, FileResource, TaskStatResource } from './resource'

export default {
  task: TaskResource,
  taskProject: TaskProjectResource,
  taskComment: TaskCommentResource,
  taskFollow: TaskFollowResource,
  taskStream: TaskStreamResource,
  tastStat: TaskStatResource,
  file: FileResource
}
